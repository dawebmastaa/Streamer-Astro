---
import { getAllPosts } from "../../lib/cosmic";
import Card from "../../components/global/Card.astro";
const data = await getAllPosts();
const type = 'blog';
---

<section class="mb-10">
  <div class="py-6 mx-6 lg:py-8">
    <p class="text-xs font-bold tracking-wide uppercase text-zinc-500">latest blog posts</p>
  </div>  
  <ul class="px-6 grid md:grid-cols-4 gap-8">
      {
        data.map((post) => (
          <Card
            title={post.title}
            href={post.slug}
            body={post.metadata.excerpt}
            image={post.metadata.cover_image.url}
            type={type}
          />
        ))
      }
    </ul>
  </section>